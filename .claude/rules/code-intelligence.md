# Claude code intelligence

Follow root `AGENTS.md` for navigation and reference-checking rules. When the
pyright LSP plugin is available, use its definition, reference, hover, and symbol
operations. Diagnostic error reminders are blockers. Otherwise use `rg`, inspect
call sites. The commit hook runs `make typecheck` for Python changes.
