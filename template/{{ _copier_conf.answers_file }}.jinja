# Managed by Copier. Change answers through Copier rather than editing this file.
# Quote strings so YAML formatters cannot interpret revisions as numbers.
{{ _copier_answers | to_nice_yaml(default_style='"') -}}
